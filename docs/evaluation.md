# Evaluation

Contrastive evaluation operates over a complete deterministic validation set and reports loss,
chance, Recall@1, Recall@5, mean rank, and median rank in both retrieval directions. Training-batch
accuracy is diagnostic only and is not a go/no-go metric.

