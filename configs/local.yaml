runtime:
  profile: local
  device: cpu
  seed: 1337
  allow_unsafe_local: false
  max_samples: 4
  epochs: 1
  num_workers: 0
  microbatch_size: 1
  contrastive_group_size: 2
data:
  clip_seconds: 2.0
  num_frames: 8
  frame_size: 64
  sample_rate: 16000
  n_mels: 64
  n_fft: 512
  hop_length: 160
  split: train
encoders:
  video_base_channels: 8
  audio_base_channels: 8
  embed_dim: 64
  projection_dim: 32
  sequence_length: 4
training:
  learning_rate: 0.0003
  weight_decay: 0.0001
  temperature: 0.07
  checkpoint_interval_seconds: 600
  max_steps: 1
paths:
  manifest_path: null
  checkpoint_dir: artifacts/checkpoints
  report_dir: artifacts/reports
remote: &remote
  manifest_uri: s3://cadence-placeholder/manifests/train.jsonl
  checkpoint_uri: s3://cadence-placeholder/checkpoints/
  requested_hardware: RTX 4090 24GB
  dependency_group: training-gpu
  python_version: "3.12"
  maximum_budget_usd: 25.0
  maximum_runtime_minutes: 360
  vps_host: null
  vast_instance_id: null

