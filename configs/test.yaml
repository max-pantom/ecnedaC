runtime:
  profile: test
  device: cpu
  seed: 7
  allow_unsafe_local: false
  max_samples: 4
  epochs: 1
  num_workers: 0
  microbatch_size: 1
  contrastive_group_size: 2
data:
  clip_seconds: 1.0
  num_frames: 4
  frame_size: 32
  sample_rate: 8000
  n_mels: 16
  n_fft: 128
  hop_length: 80
  split: train
encoders:
  video_base_channels: 4
  audio_base_channels: 4
  embed_dim: 16
  projection_dim: 8
  sequence_length: 2
training:
  learning_rate: 0.001
  weight_decay: 0.0
  temperature: 0.07
  checkpoint_interval_seconds: 600
  max_steps: 1
paths:
  manifest_path: null
  checkpoint_dir: artifacts/checkpoints
  report_dir: artifacts/reports
  intake_root: data/intake
dataset_intake:
  maximum_working_storage_gb: 1.0
  minimum_free_disk_gb: 0.01
  unknown_download_reservation_gb: 0.01
  segment_min_seconds: 0.25
  segment_max_seconds: 1.0
  segment_target_seconds: 0.5
  maximum_suggestions_per_source: 4
  ffmpeg_binary: ffmpeg
  ffprobe_binary: ffprobe
remote:
  provider: runpod
  artifact_transport: vps-ssh
  dataset_snapshot_handle: cadence-test-snapshot
  checkpoint_run_handle: cadence-test-run
  checkpoint_retention_count: 4
  requested_hardware: NVIDIA RTX A5000 24GB
  dependency_group: training-gpu
  python_version: "3.12"
  maximum_budget_usd: 1.0
  maximum_runtime_minutes: 10
  synthetic_smoke_maximum_budget_usd: 1.0
  synthetic_smoke_maximum_runtime_minutes: 10
  maximum_hourly_price_usd: 0.30
  runpod_gpu_type_id: NVIDIA RTX A5000
  runpod_gpu_count: 1
  runpod_cloud_type: COMMUNITY
  runpod_image_name: pytorch/pytorch:2.11.0-cuda12.6-cudnn9-devel
  runpod_container_disk_gb: 20
  runpod_volume_gb: 0
  runpod_pod_name: cadence-test-a5000
  vps_host: null
  vast_instance_id: null
